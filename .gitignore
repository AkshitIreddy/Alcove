node_modules
dist

# Playwright E2E artifacts
tests/e2e/.artifacts
test-results
playwright-report
# scheduled task lock
.claude/scheduled_tasks.lock

# Generated art intermediates — the shipped copies live in public/
assets/spinewalls/
assets/spinelayouts/*.png
assets/spines/

# Probe screenshots are regenerated on demand by shots-now/*.mjs.
shots-now/out/
