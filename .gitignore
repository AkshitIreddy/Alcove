node_modules
dist

# Local agent handoff — never push (owner keeps this on disk only).
HANDOFF.md
# Tauri updater signing key — private, never commit.
src-tauri/updater.key
# `dist` above does NOT cover this: the pattern matches a directory named
# exactly `dist`, and the release build writes the compiled frontend to
# `dist-release/` instead — a hundred-odd built JS, CSS and font files that
# turned up untracked and would have been swept into a commit by `git add -A`.
# Same trap `shots-now/out/` fell into.
dist-release
/NUL
/playwright-report/
/tmp-turn-*

# scheduled task lock
.claude/scheduled_tasks.lock
# Claude skills are installed globally for this machine, never vendored here.
.claude/skills/

# Generated art intermediates � the shipped copies live in public/
assets/spinewalls/
assets/spinelayouts/*.png
assets/spines/

# Local capture scratch.
shots-now/out/
shots-now/bookfit/
shots-now/flip-paper-tone/
shots-now/defaults/
shots-now/fix-*/
shots-now/welcome-*-reviewed/
shots-now/welcome-v8-final/

# Retired automated visual/audio harness output may remain on a developer's
# machine, but no QA machinery or generated report belongs in the repository.
/qa/
shots-now/**/*.png
shots-now/**/*.jpg
shots-now/**/*.jpeg
shots-now/**/*.webp
shots-now/**/*.gif
